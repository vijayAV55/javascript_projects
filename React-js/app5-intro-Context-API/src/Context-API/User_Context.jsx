import { createContext } from "react";

export const User_Context = createContext();

export const User_ContextProvider = User_Context.Provider;

export const User_Contextconsumer = User_Context.Consumer;
